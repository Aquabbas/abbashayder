---
type Props = { date: Date };

const { date } = Astro.props;
---

<!-- <time datetime={date.toISOString()}> --><!--     { --><!--         date.toLocaleDateString('en-us', { --><!--         date.toLocaleDateString('ar-iq', { --><!--         date.toLocaleDateString('sv-se', { -->
<time datetime={date.toISOString()}>
    {
        date.toLocaleDateString('en-us', {
            year: 'numeric',
            month: 'long',
            day: 'numeric'
        })
    }
</time>
